parameters:
  - id: ID
    name: ID
    kind: query
    type: string
  - id: quantity
    name: quantity
    kind: query
    type: string
source: |
  SELECT *, (
      SELECT abc
      FROM T2
      WHERE 1=2
  ) as abc FROM T1
  WHERE 1=1
  #set($valueSet = 123)
  #if($Has.ID)
  AND ID = 10
  #end
  
  #foreach($column in $Columns)
      $column.Id
  #end
  
  #if($Has.quantity)
  AND quantity = $quantity
  AND quantity = $Unsafe.quantity
  #end
hasveltysyntax: true
expressions: []
parametertypes: {}
