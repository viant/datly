// Package sql owns stable SQL primitives shared by transcription, compilation,
// and execution: source resolution, projections, tables, connections, and
// trace identity. Stateful reader and DML behavior lives in focused subpackages.
package sql
