// Package reader owns immutable SQL read plans and SQLX-backed execution with
// fresh invocation sessions, typed rows, relations, partitions, hooks, and the
// native SQLX cache path.
package reader
