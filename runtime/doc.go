// Package runtime owns registered-component lookup and small method/path
// dispatch into the unified handler engine. HTTP adaptation, SQL execution,
// artifact compilation, and component metadata live in focused packages.
package runtime
