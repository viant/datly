URI: /case005/
views:
  - table: events
    connector: db
    criteria:
      expression: user_id = ?
      params: [userID]

    bindings:
      - type: Header
        name: User-Id
        placeholder: userID
        default: -1