pipeline:
  test:
    description: parent join-on optimization is expanded inside child UNION branches
    action: http/runner:send
    requests:
      - Method: GET
        URL: http://127.0.0.1:8080/v1/api/shape/dev/col/vendors/
        Expect:
          Code: 200
